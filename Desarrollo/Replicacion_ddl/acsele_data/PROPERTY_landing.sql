CREATE OR REPLACE EXTERNAL TABLE `iter-data-storage-pv-uat.acsele_temp.PROPERTY_landing`
(
  propertyid NUMERIC,
  simbolo STRING,
  type NUMERIC,
  etiqueta STRING,
  tipo STRING,
  state STRING,
  filtertransformer STRING,
  propertyinfo STRING,
  validatorfunction STRING,
  formula STRING,
  transforfunction STRING,
  tabletransformer STRING,
  visible NUMERIC,
  eraseable NUMERIC,
  resetable NUMERIC,
  editable NUMERIC,
  amendable NUMERIC,
  secondpass NUMERIC,
  temporary NUMERIC,
  typerange STRING,
  presenterfunction STRING,
  rule STRING,
  minvalue FLOAT64,
  maxvalue FLOAT64,
  concept STRING,
  mask STRING,
  uniquekey STRING,
  multiplantilla STRING,
  precisiontype NUMERIC,
  validator STRING,
  defaultvalue STRING,
  colinputtransformer STRING,
  colvaluetransformer STRING,
  parent STRING,
  numdecimal NUMERIC,
  multiformula STRING,
  multicolumna STRING,
  pty_sec_visible NUMERIC,
  pty_sec_editable NUMERIC,
  description STRING,
  pty_touaa NUMERIC,
  nro_dias STRING,
  status STRING,
  substatus STRING,
  prt_bigdata NUMERIC,
  prt_clause NUMERIC,
  prt_visibilityindocuments NUMERIC,
  prt_orderfunction STRING
)OPTIONS (format="PARQUET",URIS=["gs://interseguro-datalake-alloy-uat-new/acsele_data/PROPERTY_landing/*.parquet"]);